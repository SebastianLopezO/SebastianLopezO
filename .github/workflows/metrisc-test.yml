name: Test Metrics

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      - name: test
        uses: lowlighter/metrics@latest
        with:
          template: repository
          user: SmartPotTech
          repo: SmartPot-Web
