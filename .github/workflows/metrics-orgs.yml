name: Organization Metrics

on:
  schedule:
    - cron: "0 1 * * *"
  workflow_dispatch:

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:

      # =============================
      # 🏢 ADCOM-Group
      # =============================
      - name: ADCOM-Group
        uses: lowlighter/metrics@v3.34
        with:
          filename: metrics.org.adcom-group.svg
          token: ${{ secrets.METRICS_TOKEN }}
          template: organization
          user: ADCOM-Group
          base: header, activity, community, repositories

      # =============================
      # 🏢 EngineeringSebastian-s
      # =============================
      - name: EngineeringSebastian-s
        uses: lowlighter/metrics@v3.34
        with:
          filename: metrics.org.engineeringsebastians.svg
          token: ${{ secrets.METRICS_TOKEN }}
          template: organization
          user: EngineeringSebastian-s
          base: header, activity, community, repositories

      # =============================
      # 🏢 PepperTechDev
      # =============================
      - name: PepperTechDev
        uses: lowlighter/metrics@v3.34
        with:
          filename: metrics.org.peppertechdev.svg
          token: ${{ secrets.METRICS_TOKEN }}
          template: organization
          user: PepperTechDev
          base: header, activity, community, repositories

      # =============================
      # 🏢 SmartPotTech
      # =============================
      - name: SmartPotTech
        uses: lowlighter/metrics@v3.34
        with:
          filename: metrics.org.smartpottech.svg
          token: ${{ secrets.METRICS_TOKEN }}
          template: organization
          user: SmartPotTech
          base: header, activity, community, repositories

      # =============================
      # 🏢 TaurusSebastian-s
      # =============================
      - name: TaurusSebastian-s
        uses: lowlighter/metrics@v3.34
        with:
          filename: metrics.org.taurussebastians.svg
          token: ${{ secrets.METRICS_TOKEN }}
          template: organization
          user: TaurusSebastian-s
          base: header, activity, community, repositories

      # =============================
      # 🏢 TechnicalSebastians
      # =============================
      - name: TechnicalSebastians
        uses: lowlighter/metrics@v3.34
        with:
          filename: metrics.org.technicalsebastians.svg
          token: ${{ secrets.METRICS_TOKEN }}
          template: organization
          user: TechnicalSebastians
          base: header, activity, community, repositories

      # =============================
      # 🏢 TestLabData
      # =============================
      - name: TestLabData
        uses: lowlighter/metrics@v3.34
        with:
          filename: metrics.org.testlabdata.svg
          token: ${{ secrets.METRICS_TOKEN }}
          template: organization
          user: TestLabData
          base: header, activity, community, repositories

      # =============================
      # 🏢 TivenosEducation
      # =============================
      - name: TivenosEducation
        uses: lowlighter/metrics@v3.34
        with:
          filename: metrics.org.tivenoseducation.svg
          token: ${{ secrets.METRICS_TOKEN }}
          template: organization
          user: TivenosEducation
          base: header, activity, community, repositories

      # =============================
      # 🏢 VocationalSebastian-s
      # =============================
      - name: VocationalSebastian-s
        uses: lowlighter/metrics@v3.34
        with:
          filename: metrics.org.vocationalsebastians.svg
          token: ${{ secrets.METRICS_TOKEN }}
          template: organization
          user: VocationalSebastian-s
          base: header, activity, community, repositories
